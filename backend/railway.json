{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "apt install python3-pip libpango-1.0-0 && apt install weasyprint && python manage.py runserver",
        "restartPolicyType": "NEVER",
        "restartPolicyMaxRetries": 10
    }
}